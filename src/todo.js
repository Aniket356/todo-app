function ToDo(title, description, dueDate, isImportant, completed){
    return {
        title,
        description,
        dueDate,
        isImportant,
        completed,
    };
}

export default ToDo;